"""GitHub integration (connection + repo/activity sync + solution pushes).

Implemented across Phases 2-5 and 10 of the integrations milestone:
auth.py (connect OAuth flow), client.py (API calls), sync.py (data sync).
"""
